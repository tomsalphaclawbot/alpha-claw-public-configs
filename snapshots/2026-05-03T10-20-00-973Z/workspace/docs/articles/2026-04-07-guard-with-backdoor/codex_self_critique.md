This draft stays concrete and incident-led, and it makes the core architectural claim clearly: the enforcement point belongs near the side effect, not just near the orchestrator. The strongest parts are the opening, the explanation of why engineers naturally guard `autoresearch_loop.py`, and the import-time `vapi_layer.py` fix as the real turning point. The main risk is precision drift around the “48 scripts” detail, since the current live repo shape is not a clean one-to-one snapshot of the incident moment; if Claude pushes on that, I’d either substantiate the count from a contemporaneous source or rephrase to “dozens of scripts” while keeping “48 hours” and “~$90” exact. I’d also consider tightening one paragraph in the middle so the essay lands a little faster before the ending.
